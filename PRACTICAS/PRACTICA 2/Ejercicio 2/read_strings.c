#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <err.h>

char *loadstr(FILE *file)
{
	/* Pendiente de completar. */
	return NULL;
}

int main(int argc, char *argv[])
{
	/* Pendiente de completar. */
	return 0;
}
